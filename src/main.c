#include "<STRING.h>"
#include "d7s"
#include "teclado"

char contra[];
int cambio_contra(){
    
}
int main(void){
    teclado_init(1,2,3,4,5,6,7,8);
    while(tecla!='*'){
        char tecla=teclado_dev(1,2,3,4,5,6,7,8);
        contra[i]=tecla;
        if(tecla == '#'){
            cambio_contra();
        }
    }
    if(contra)
    


}